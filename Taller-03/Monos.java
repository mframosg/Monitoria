public class Monos {
    public boolean problemaMonos(boolean sonrie_a, boolean sonrie_b) {
        if ((sonrie_a && sonrie_b) ||(!sonrie_a && !sonrie_b) )return true;
        else return false;
    }
}